name: Quality / SonarQube

# Counterpart of the GitLab Sonarqube job. Runs in the SonarSource scanner image
# and waits on the quality gate, so a failing gate fails the run.

on:
  workflow_call:
    inputs:
      project-version:
        required: false
        type: string
        default: ""
        description: "Version reported to SonarQube, from init.yml's tag."
      project-key:
        required: false
        type: string
        default: ""
        description: "SonarQube project key. Defaults to vars.SONAR_PROJECT_KEY, then owner_repo."
      project-path:
        required: false
        type: string
        default: ""
        description: "Project root. Monorepo children set this; the key is suffixed with it."
      wait-for-quality-gate:
        required: false
        type: boolean
        default: true
        description: "Fail the job when the quality gate fails."

jobs:
  sonarqube:
    name: SonarQube
    runs-on: ${{ vars.CI_RUNNER || 'ubuntu-26.04' }}
    timeout-minutes: 45
    permissions:
      contents: read
    container:
      image: ${{ vars.IMAGE_REGISTRY }}/${{ vars.SONAR_SCANNER_IMAGE }}
    env:
      SONAR_USER_HOME: ${{ github.workspace }}/.sonar
      SONAR_SCANNER_OPTS: -XX:MaxRAMPercentage=90.0 -XX:InitialRAMPercentage=90.0
    defaults:
      run:
        working-directory: ${{ inputs.project-path || vars.PROJECT_PATH || '.' }}

    steps:
      # Sonar needs full history to attribute issues and measure new code.
      - name: Checkout Full History
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Download Test & Coverage Reports
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          pattern: "*-test-reports"
          merge-multiple: true
          path: ${{ inputs.project-path || vars.PROJECT_PATH || '.' }}

      - name: Cache Sonar Analysis Data
        id: cache
        uses: actions/cache/restore@v6
        with:
          path: .sonar
          key: sonar-${{ github.repository_id }}
          restore-keys: sonar-${{ github.repository_id }}

      - name: Validate SonarQube Configuration
        env:
          SONAR_URL: ${{ vars.SONAR_URL }}
          SONARQUBE_TOKEN: ${{ secrets.SONARQUBE_TOKEN }}
        run: |
          set -euo pipefail
          MISSING=()
          [[ -n "${SONAR_URL}" ]] || MISSING+=("vars.SONAR_URL")
          [[ -n "${SONARQUBE_TOKEN}" ]] || MISSING+=("secrets.SONARQUBE_TOKEN")
          if [[ ${#MISSING[@]} -gt 0 ]]; then
            echo "::error title=SonarQube::Missing configuration: ${MISSING[*]}"
            {
              echo "### 🔍 SonarQube"
              echo ""
              echo "❌ No analysis was run. These are not set on this repository or organisation:"
              echo ""
              # shellcheck disable=SC2016 # backticks are markdown, not command substitution
              printf -- '- `%s`\n' "${MISSING[@]}"
              echo ""
            } >> "${GITHUB_STEP_SUMMARY}"
            exit 1
          fi

      - name: Run Analysis & Quality Gate
        env:
          SONAR_URL: ${{ vars.SONAR_URL }}
          SONAR_TOKEN: ${{ secrets.SONARQUBE_TOKEN }}
          PROJECT_KEY_INPUT: ${{ inputs.project-key }}
          VAR_PROJECT_KEY: ${{ vars.SONAR_PROJECT_KEY }}
          PROJECT_VERSION: ${{ inputs.project-version }}
          SUBPATH: ${{ inputs.project-path }}
          WAIT: ${{ inputs.wait-for-quality-gate }}
        run: |
          set -euo pipefail

          PROJECT_KEY="${PROJECT_KEY_INPUT:-${VAR_PROJECT_KEY:-${GITHUB_REPOSITORY//\//_}}}"
          if [[ -n "${SUBPATH}" && "${SUBPATH}" != "." ]]; then
            SLUG="$(tr -c 'a-zA-Z0-9_.:-' '-' <<<"${SUBPATH}" | sed 's/-\+/-/g;s/^-//;s/-$//')"
            PROJECT_KEY="${PROJECT_KEY}_${SLUG}"
          fi
          echo "Analysing as project key '${PROJECT_KEY}'."

          SCANNER_ARGS=(
            "-Dsonar.projectKey=${PROJECT_KEY}"
            "-Dsonar.host.url=${SONAR_URL}"
            "-Dsonar.token=${SONAR_TOKEN}"
            "-Dsonar.qualitygate.wait=${WAIT}"
            "-Dsonar.links.ci=${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions"
            "-Dsonar.links.scm=${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}"
          )
          [[ -n "${PROJECT_VERSION}" ]] && SCANNER_ARGS+=("-Dsonar.projectVersion=${PROJECT_VERSION}")
          [[ -f sonar.properties ]] && SCANNER_ARGS+=("-Dproject.settings=sonar.properties")

          # Exported before the scanner runs: the summary needs the key even on failure.
          echo "SONAR_PROJECT_KEY=${PROJECT_KEY}" >> "${GITHUB_ENV}"

          set +e
          sonar-scanner "${SCANNER_ARGS[@]}" 2>&1 | tee "${RUNNER_TEMP}/sonar-scanner.log"
          SCANNER_RESULT="${PIPESTATUS[0]}"
          set -e
          exit "${SCANNER_RESULT}"

      # GitHub caches are immutable, so a stable key freezes at whatever the first
      # run stored. Delete the entry before rewriting it, at most once a day.
      # Needs `actions: write`; without it the older entry survives and restores.
      - name: Refresh Cache Entry
        id: refresh
        if: ${{ !cancelled() }}
        env:
          GH_TOKEN: ${{ github.token }}
          CACHE_KEY: sonar-${{ github.repository_id }}
        run: |
          set -euo pipefail

          STAMP=".sonar/.cached-on"
          TODAY="$(date -u +%Y-%m-%d)"

          # The caches API matches `key` as a PREFIX, so filter for the exact key.
          EXISTING="$(gh api "repos/${GITHUB_REPOSITORY}/actions/caches?key=${CACHE_KEY}" \
            --jq "[.actions_caches[] | select(.key == \"${CACHE_KEY}\")] | length" 2>/dev/null || echo 0)"

          if [[ "${EXISTING}" == "0" ]]; then
            mkdir -p .sonar && echo "${TODAY}" > "${STAMP}"
            echo "save=true" >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          if [[ -f "${STAMP}" ]] && [[ "$(cat "${STAMP}")" == "${TODAY}" ]]; then
            echo "Analysis cache was already refreshed today; keeping it."
            echo "save=false" >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          mkdir -p .sonar && echo "${TODAY}" > "${STAMP}"
          if gh api -X DELETE "repos/${GITHUB_REPOSITORY}/actions/caches?key=${CACHE_KEY}" >/dev/null 2>&1; then
            echo "save=true" >> "${GITHUB_OUTPUT}"
          else
            echo "::warning title=Sonar cache::Could not replace '${CACHE_KEY}'. Grant 'actions: write' in the calling workflow to let it refresh."
            echo "save=false" >> "${GITHUB_OUTPUT}"
          fi

      - name: Save Sonar Analysis Cache
        if: ${{ !cancelled() && steps.refresh.outputs.save == 'true' }}
        uses: actions/cache/save@v6
        with:
          path: .sonar
          key: sonar-${{ github.repository_id }}

      - name: Publish Quality Summary
        if: always()
        env:
          OUTCOME: ${{ job.status }}
          DASHBOARD: ${{ vars.SONAR_EXTERNAL_URL || vars.SONAR_URL }}
        run: |
          set -euo pipefail
          LOG="${RUNNER_TEMP}/sonar-scanner.log"
          GATE="$(grep -aE 'QUALITY GATE STATUS|^\s*-\s' "${LOG}" 2>/dev/null | tail -n 12 || true)"
          {
            echo "### 🔍 SonarQube"
            echo ""
            case "${OUTCOME}" in
              success) echo "✅ Analysis complete and the quality gate passed." ;;
              failure) echo "❌ Analysis failed or the quality gate was not met." ;;
              *)       echo "Status: ${OUTCOME}" ;;
            esac
            echo ""
            # Unset when the run never got past configuration validation.
            if [[ -n "${SONAR_PROJECT_KEY:-}" ]]; then
              KEY_CELL="\`${SONAR_PROJECT_KEY}\`"
              [[ -z "${DASHBOARD}" ]] || KEY_CELL="[${KEY_CELL}](${DASHBOARD}/dashboard?id=${SONAR_PROJECT_KEY})"
              echo "| Property | Value |"
              echo "|---|---|"
              echo "| **Project key** | ${KEY_CELL} |"
            fi
            if [[ -n "${GATE}" ]]; then
              echo ""
              echo '```'
              echo "${GATE}"
              echo '```'
            elif [[ "${OUTCOME}" != "success" && -s "${LOG}" ]]; then
              echo ""
              echo '```'
              tail -n 30 "${LOG}"
              echo '```'
            fi
            echo ""
          } >> "${GITHUB_STEP_SUMMARY}"
