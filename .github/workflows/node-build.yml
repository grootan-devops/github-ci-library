name: Node / Dependency, Build & Test

# GitLab counterpart: Node:Dependency:Download -> Node:Build + Node:Test:Unit.
# No production-install job: the packaging Dockerfile installs offline from the
# npm cache, so re-running `npm ci --omit=dev` here would prove nothing.

on:
  workflow_call:
    inputs:
      project-path:
        required: false
        type: string
        default: ""
        description: "Path to the Node project root. Monorepo children set this."
      build-command:
        required: false
        type: string
        default: "npm run build"
        description: "Build command."
      test-command:
        required: false
        type: string
        default: "npm test"
        description: "Unit test command. Must write JUnit XML into test-report/."
      build-artifact-path:
        required: false
        type: string
        default: "dist"
        description: "Directory produced by the build command."
      run-tests:
        required: false
        type: boolean
        default: true
        description: "Run the unit test job."
      build-env:
        required: false
        type: string
        default: ""
        description: "Newline-separated KEY=VALUE pairs exported into build and test."

env:
  PROJECT_PATH: ${{ inputs.project-path || vars.PROJECT_PATH || '.' }}
  TEST_REPORT_DIR: test-report
  npm_config_cache: ${{ github.workspace }}/.npm

jobs:
  dependency:
    name: Dependency Download
    runs-on: ${{ vars.CI_RUNNER || 'ubuntu-26.04' }}
    timeout-minutes: 20
    permissions:
      contents: read
    container:
      image: registry-1.docker.io/grootantech/toolkit:verify-v2
    defaults:
      run:
        working-directory: ${{ inputs.project-path || vars.PROJECT_PATH || '.' }}
    outputs:
      cache-key: ${{ steps.key.outputs.key }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      # hashFiles returns "" for a missing package-lock.json, collapsing the key onto the bare `node-` prefix.
      - name: Compute Dependency Cache Key
        id: key
        run: echo "key=node-${{ hashFiles(format('{0}/package-lock.json', inputs.project-path || vars.PROJECT_PATH || '.')) || 'nolock' }}" >> "${GITHUB_OUTPUT}"

      - name: Restore npm Cache
        id: cache
        uses: actions/cache/restore@v6
        with:
          path: .npm
          key: ${{ steps.key.outputs.key }}
          restore-keys: node-

      - name: Download Dependencies
        id: download
        run: |
          set -uo pipefail
          npm ci --include=dev --prefer-offline --no-audit --no-fund 2>&1 | tee "${RUNNER_TEMP}/npm-ci.log"
          exit "${PIPESTATUS[0]}"

      # Cache entries are immutable: saving after a failed install would freeze a
      # half-populated tree under a valid key until package-lock.json changes.
      - name: Save npm Cache
        if: ${{ success() && steps.cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@v6
        with:
          path: .npm
          key: ${{ steps.key.outputs.key }}

      - name: Publish Dependency Summary
        if: always()
        env:
          CACHE_HIT: ${{ steps.cache.outputs.cache-hit }}
          CACHE_KEY: ${{ steps.key.outputs.key }}
          DOWNLOAD_OUTCOME: ${{ steps.download.outcome }}
        run: |
          set -euo pipefail
          # Anything else means an earlier step failed and the download never ran.
          [[ "${DOWNLOAD_OUTCOME}" =~ ^(success|failure)$ ]] || exit 0
          CACHE_STATE="rebuilt — no entry matched"
          [[ "${CACHE_HIT}" == "true" ]] && CACHE_STATE="restored"
          {
            echo "### 📗 Node dependencies"
            echo ""
            if [[ "${DOWNLOAD_OUTCOME}" == "success" ]]; then
              echo "✅ Dependencies installed. Cache \`${CACHE_KEY}\`: ${CACHE_STATE}."
            else
              echo "❌ \`npm ci\` failed. Cache \`${CACHE_KEY}\`: ${CACHE_STATE}. npm reported:"
              echo ""
              echo '```'
              tail -n 50 "${RUNNER_TEMP}/npm-ci.log" 2>/dev/null || echo "(no output was captured)"
              echo '```'
            fi
            echo ""
          } >> "${GITHUB_STEP_SUMMARY}"

  build:
    name: Build
    needs: dependency
    runs-on: ${{ vars.CI_RUNNER || 'ubuntu-26.04' }}
    timeout-minutes: 30
    permissions:
      contents: read
    container:
      image: registry-1.docker.io/grootantech/toolkit:verify-v2
    defaults:
      run:
        working-directory: ${{ inputs.project-path || vars.PROJECT_PATH || '.' }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Export Build Environment
        if: ${{ inputs.build-env != '' }}
        env:
          BUILD_ENV: ${{ inputs.build-env }}
        run: |
          set -euo pipefail
          while IFS='=' read -r KEY VAL; do
            KEY="${KEY#"${KEY%%[![:space:]]*}"}"
            KEY="${KEY%"${KEY##*[![:space:]]}"}"
            [[ -z "${KEY}" || "${KEY}" =~ ^# ]] && continue
            printf '%s=%s\n' "${KEY}" "${VAL}" >> "${GITHUB_ENV}"
          done <<< "${BUILD_ENV}"

      - name: Restore npm Cache
        uses: actions/cache/restore@v6
        with:
          path: .npm
          key: ${{ needs.dependency.outputs.cache-key }}
          restore-keys: node-

      - name: Install Dependencies
        run: npm ci --include=dev --prefer-offline --no-audit --no-fund

      - name: Build
        id: compile
        run: |
          set -uo pipefail
          # Braces, not a bare pipe: a command ending in its own redirect would leave tee empty.
          { ${{ inputs.build-command }} ; } 2>&1 | tee "${RUNNER_TEMP}/build.log"
          exit "${PIPESTATUS[0]}"

      - name: Upload Build Output
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v7
        with:
          name: node-dist
          path: ${{ inputs.project-path || vars.PROJECT_PATH || '.' }}/${{ inputs.build-artifact-path }}/
          if-no-files-found: ignore
          retention-days: 7
          include-hidden-files: true

      - name: Publish Build Summary
        if: always()
        env:
          OUT_DIR: ${{ inputs.build-artifact-path }}
          BUILD_COMMAND: ${{ inputs.build-command }}
          COMPILE_OUTCOME: ${{ steps.compile.outcome }}
        run: |
          set -euo pipefail
          # Anything else means an earlier step failed and the build never ran.
          [[ "${COMPILE_OUTCOME}" =~ ^(success|failure)$ ]] || exit 0
          FILE_COUNT="$(find "${OUT_DIR}" -type f 2>/dev/null | wc -l | tr -d ' ')" || FILE_COUNT=0
          {
            echo "### 📦 Node build"
            echo ""
            if [[ "${COMPILE_OUTCOME}" != "success" ]]; then
              echo "❌ \`${BUILD_COMMAND}\` failed. It reported:"
              echo ""
              echo '```'
              tail -n 50 "${RUNNER_TEMP}/build.log" 2>/dev/null || echo "(no output was captured)"
              echo '```'
            elif [[ "${FILE_COUNT}" -gt 0 ]]; then
              echo "✅ Output \`${OUT_DIR}/\` — $(du -sh "${OUT_DIR}" | cut -f1) across ${FILE_COUNT} files."
            else
              echo "⚠️ \`${BUILD_COMMAND}\` succeeded but wrote nothing to \`${OUT_DIR}/\`, so the \`node-dist\` artifact will be empty."
            fi
            echo ""
          } >> "${GITHUB_STEP_SUMMARY}"

  test:
    name: Unit Test
    if: ${{ inputs.run-tests }}
    needs: dependency
    runs-on: ${{ vars.CI_RUNNER || 'ubuntu-26.04' }}
    timeout-minutes: 30
    permissions:
      contents: read
      checks: write
    container:
      image: registry-1.docker.io/grootantech/toolkit:verify-v2
    defaults:
      run:
        working-directory: ${{ inputs.project-path || vars.PROJECT_PATH || '.' }}
    env:
      NODE_ENV: test
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Checkout CI Library
        uses: actions/checkout@v7
        with:
          repository: ${{ job.workflow_repository }}
          ref: ${{ job.workflow_sha }}
          path: .ci-library
          token: ${{ secrets.CI_LIBRARY_TOKEN || github.token }}

      - name: Export Test Environment
        if: ${{ inputs.build-env != '' }}
        env:
          BUILD_ENV: ${{ inputs.build-env }}
        run: |
          set -euo pipefail
          while IFS='=' read -r KEY VAL; do
            KEY="${KEY#"${KEY%%[![:space:]]*}"}"
            KEY="${KEY%"${KEY##*[![:space:]]}"}"
            [[ -z "${KEY}" || "${KEY}" =~ ^# ]] && continue
            printf '%s=%s\n' "${KEY}" "${VAL}" >> "${GITHUB_ENV}"
          done <<< "${BUILD_ENV}"

      - name: Restore npm Cache
        uses: actions/cache/restore@v6
        with:
          path: .npm
          key: ${{ needs.dependency.outputs.cache-key }}
          restore-keys: node-

      - name: Install Dependencies
        run: npm ci --include=dev --prefer-offline --no-audit --no-fund

      - name: Run Unit Tests
        id: unit
        run: |
          set -uo pipefail
          mkdir -p "${TEST_REPORT_DIR}" coverage
          # Braces, not a bare pipe: a command ending in its own redirect would leave tee empty.
          { ${{ inputs.test-command }} ; } 2>&1 | tee "${RUNNER_TEMP}/test.log"
          exit "${PIPESTATUS[0]}"

      # junit-report.sh has nothing to show when the runner never wrote a JUnit file.
      - name: Explain Test Runner Failure
        if: ${{ failure() && steps.unit.outcome == 'failure' }}
        env:
          TEST_COMMAND: ${{ inputs.test-command }}
        run: |
          set -euo pipefail
          {
            echo "### 🧪 Node test command"
            echo ""
            echo "❌ \`${TEST_COMMAND}\` exited non-zero. It reported:"
            echo ""
            echo '```'
            tail -n 50 "${RUNNER_TEMP}/test.log" 2>/dev/null || echo "(no output was captured)"
            echo '```'
            echo ""
            echo "Per-case results, where the run got far enough to produce any, are in the table below and in the \`node-test-reports\` artifact."
            echo ""
          } >> "${GITHUB_STEP_SUMMARY}"

      - name: Upload Test & Coverage Reports
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v7
        with:
          name: node-test-reports
          path: |
            ${{ inputs.project-path || vars.PROJECT_PATH || '.' }}/test-report/
            ${{ inputs.project-path || vars.PROJECT_PATH || '.' }}/coverage/
          if-no-files-found: ignore
          retention-days: 7

      - name: Publish Test Report
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          CHECK_NAME: "Node Unit Test Results"
          REPORT_GLOB: "${{ inputs.project-path || vars.PROJECT_PATH || '.' }}/test-report/**/*.xml"
          JOB_SUMMARY: "true"
          FAIL_ON_FAILURE: "true"
        run: bash "${GITHUB_WORKSPACE}/.ci-library/scripts/junit-report.sh"
